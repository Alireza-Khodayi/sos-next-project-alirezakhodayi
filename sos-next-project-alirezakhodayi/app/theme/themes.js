"use client";
import { createTheme } from "@mui/material/styles";

let theme = createTheme({
  palette: {
    primary: {
      main: "#1158A7",
    },
    // secondary: {
    //   main: "#edf2ff",
    // },
  },
});

export default theme;
